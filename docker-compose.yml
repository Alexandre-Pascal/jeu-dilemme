services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dilemme
    volumes:
      - dilemme_pg:/var/lib/postgresql/data

volumes:
  dilemme_pg:
